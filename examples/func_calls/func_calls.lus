node incr(x: int) returns (y: int);
var l: int;
let
    l = x + 1;
    y = x + l;
tel

node compute(a, b: int) returns (c: int);
var z, t: int;
let
    z = incr(a);
    t = incr(b);
    c = z + t;
tel

node check() returns (ok: bool);
var r, s: int;
let
    r = 1;
    s = 1;
    ok = compute(r, s) >= compute(s, r);
tel